class Details:
    def __init__(self, name, text, prediction):
        self.name = name
        self.text = text
        self.prediction = prediction

        #
        # $('li').css('cursor', 'pointer');
        # $(document).ready(
        #     function()
        # {
        # $('li').click(
        #     function()
        # {
        # $(this).css("font-weight", "bold");
        # console.log($(this).text());
        # }
        # );
        # });








        # self.image = image
        # self.word_cloud = word_cloud
        # < !--    $("#{{item.name}}").click(function()
        # {-->
        # < !--          $(this).addClass("btn");
        # -->
        # < !--          $(this).attr('font-size': 32);--> <!--list-group-item-primary -->
        # });$('#{{}}').css('font-weight', 'bold');
        #
        # < !-- < div
        #
        # class ="card" > -->
        #
        # < !-- < ul
        #
        # class ="list-group list-group-flush" > -->
        #
        # < !--        { %
        # for item in results %}-->
        # < !-- < li
        #
        # class ="list-group-item pointer" id="{{item.name}}" > -->
        #
        # < !-- < div
        #
        # class ="row" > -->
        #
        # < !-- < div
        #
        # class ="col-8 center" > -->
        #
        # < !-- < strong > {{item.name}} < / strong > -->
        # < !-- < / div > -->
        # < !-- < / div > -->
        # < !-- < div
        #
        # class ="row" > -->
        #
        # < !-- < div
        #
        # class ="col-8 center" > -->
        #
        # < !-- < img
        # src = "{{item.word_cloud.url}}"
        # height = "200"
        # width = "200" > -->
        # < !-- < / div > -->
        # < !-- < / div > -->
        #
        # < !-- < / li > -->
        # < !-- < hr
        #
        # class ="mt-0 mb-4" > -->
        #
        # < !--        { % endfor %}-->
        # < !-- < / ul > -->
        # < !-- < / div > -->


